generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Waitlist {
  id        String   @id @default(cuid())
  email     String   @unique
  phone     String?
  createdAt DateTime @default(now())
}

model PartnerApplication {
  id           String   @id @default(cuid())
  companyName  String
  contactName  String
  email        String
  phone        String?   // keep optional at DB; enforce required via API going forward
  location     String?   // new; enforce required via API going forward
  notes        String?
  createdAt    DateTime  @default(now())
}
